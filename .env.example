# Supafolio - environment variables template
# Copy to .env (local dev) or set in Vercel/hosting (production). Never commit .env or .env.production.
#
# === SECURITY ===
# - Only VITE_* variables are embedded in the client bundle. Use VITE_ only for non-secrets (publishable keys, anon key, URLs).
# - Never use VITE_ for: CLERK_SECRET_KEY, SUPABASE_SERVICE_ROLE_KEY, MISTRAL_API_KEY, or any secret.
# - Keep .env and .env.production in .gitignore. Use this file as the only committed env reference.

# Data source (mock = in-memory dev; supabase = real DB)
VITE_DATA_SOURCE=supabase

# Supabase (client-safe; RLS protects data)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=

# Clerk - publishable key only (client-safe). Use pk_test_ for dev, pk_live_ for production.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_test_key_here

# Clerk - server/script only (NEVER add VITE_ prefix)
CLERK_DOMAIN=clerk.supafolio.app
CLERK_SIGN_IN_URL=/sign-in
CLERK_SIGN_UP_URL=/sign-up
CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/dashboard
CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/dashboard
# CLERK_SECRET_KEY=           # Server/scripts only. Never commit.
# CLERK_INSTANCE_ID=          # Optional; from Clerk Dashboard

# Supabase - server/scripts only (NEVER add VITE_ prefix)
# SUPABASE_SERVICE_ROLE_KEY= # Bypasses RLS. Only for server/scripts. Never commit.

# Edge functions / server-only (e.g. Supabase Edge Function secrets)
# MISTRAL_API_KEY=            # Set in Supabase Dashboard for process-statement. Never commit.
